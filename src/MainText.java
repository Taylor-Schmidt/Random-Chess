/**
 * This class runs the program.
 * Creates a new instance of GameManager and calls its run() method.
 */
public class MainText {
    public static void main(String[] args) {
        GameManagerText gameManager = new GameManagerText();
        gameManager.run();
    }
}
